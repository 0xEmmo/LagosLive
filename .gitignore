node_modules
.next
out
.env*.local
# Real secrets, not templates. Only .env.example is tracked; a local .env holds
# the Supabase service-role key and the Paystack secret key, and `git add -A`
# would otherwise pick it up without complaint.
.env
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
supabase/.temp
